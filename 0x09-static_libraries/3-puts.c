#include "main.h"
void _puts(char *str)
#include <stdio.h>
/**
 * main - Entry point
 * puts -  Programming is like building a multilingual puzzle
 * Return: Always 0 for success
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
