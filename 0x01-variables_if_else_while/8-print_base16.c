#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - Entry Point
 *
 * Return -  will return 0 for success
 */
int main(void)
{
	int num;
	char base_16;

	for (num = '0'; num <= '9'; num++)
	putchar(num);
	for (base_16 = 'a'; base_16 <= 'f'; base_16++)
		putchar(base_);
		putchar('\n');
		return (0);
}
